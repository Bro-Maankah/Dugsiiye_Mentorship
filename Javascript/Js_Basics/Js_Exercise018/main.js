// JavaScript Exercise #18: Ternary Operator

//Use the ternary operator to log "Pass" if the variable grade is 60 or more, and "Fail" otherwise.

let grade = 67;
let message = grade >= 60 ? "Pass" : "Fail.";
console.log(message);